# Copy to .env and set a local password. Never commit .env.
# Consumed by docker-compose.yml for the local PostgreSQL container.

POSTGRES_USER=agentboard
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=agentboard
POSTGRES_PORT=5432
