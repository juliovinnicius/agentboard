# AgentBoard Web

Next.js (App Router) client. Setup, environment variables and URLs are
documented in the [repository README](../README.md).

```bash
npm run dev         # http://localhost:3000
npm run lint
npm run typecheck
npm run build
```
